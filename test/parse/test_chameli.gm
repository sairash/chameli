name = "";

nil = "nil" | "null" | "sunna";
bool = "true" | "false";
string = String(");

char = string(', 1)

exp = Number | nil | bool | string | name | char;

functionCall = name + "(" + :{exp + ','} | [exp] + ")";
